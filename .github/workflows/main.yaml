name: main
on: push

jobs:
  build-and-deploy:
    uses: navikt/isworkflows/.github/workflows/node-build-deploy.yml@master
    with:
      dev-cluster: dev-fss
      prod-cluster: prod-fss
    secrets: inherit
