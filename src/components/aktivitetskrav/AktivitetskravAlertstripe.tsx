import { Alert } from "@navikt/ds-react";
import styled from "styled-components";

export const AktivitetskravAlertstripe = styled(Alert)`
  margin-bottom: 1em;

  .navds-alert__wrapper {
    max-width: 80%;
  }
`;
